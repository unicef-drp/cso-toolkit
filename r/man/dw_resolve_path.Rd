% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dw_io.R
\name{dw_resolve_path}
\alias{dw_resolve_path}
\title{Resolve a logical DW path to a filesystem path}
\usage{
dw_resolve_path(
  path = NULL,
  name = NULL,
  sector = NULL,
  kind = c("wrk", "raw", "meta"),
  vintage = NULL
)
}
\arguments{
\item{path}{Character. Literal subpath to append to the kind's root.
Mutually exclusive with `name` / `sector` / `vintage`.}

\item{name}{Character. File basename. Used together with `sector` and
optionally `vintage` to build the subpath.}

\item{sector}{Character. Sector folder name (e.g. `"ed"`, `"nt"`). Only
used when `name` is supplied.}

\item{kind}{Character. One of `"wrk"`, `"raw"`, `"meta"`. Selects which
profile-defined root to resolve against.}

\item{vintage}{Character. Optional subfolder (e.g. `"2026-05"` or
`"2026-05-23"`) inserted between `sector` and `name`.}
}
\value{
Character. Absolute filesystem path.
}
\description{
Two call styles are supported:
\itemize{
  \item Path-string: `dw_resolve_path(path = "ed/dw_ed_edu.csv", kind = "wrk")`
  \item Structured: `dw_resolve_path(name = "dw_ed_edu.csv", sector = "ed", kind = "wrk")`
}
}
\examples{
\dontrun{
dw_resolve_path(name = "dw_ed_edu.csv", sector = "ed", kind = "wrk")
dw_resolve_path(path = "ed/dw_ed_edu.csv", kind = "wrk")
}
}
