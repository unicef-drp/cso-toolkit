% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dw_io.R
\name{dw_compare}
\alias{dw_compare}
\title{Compare a current dataset against a reference (added / removed / changed)}
\usage{
dw_compare(
  current,
  reference,
  by,
  value_cols = NULL,
  numeric_value_cols = NULL,
  tol = 1e-05,
  label = "compare",
  write_report_to = NULL,
  verbose = NULL,
  debug = NULL
)
}
\arguments{
\item{current}{Data frame or character path to a file. The "new" side.}

\item{reference}{Data frame or character path to a file. The "old" side.}

\item{by}{Character vector of key columns.}

\item{value_cols}{Character vector of columns to value-compare. Default
`NULL` (= all non-key columns present on both sides).}

\item{numeric_value_cols}{Subset of `value_cols` to treat as numeric
(uses `tol`). Others are string-compared.}

\item{tol}{Numeric tolerance for numeric value comparisons. Default
`1e-5`.}

\item{label}{Character. Label used in the summary row and report
filenames. Default `"compare"`.}

\item{write_report_to}{Character. Directory to write
`<label>_summary.csv`, `<label>_added_rows.csv`,
`<label>_removed_rows.csv`, `<label>_changed_rows.csv`. Default
`NULL` (don't write).}

\item{verbose}{Logical or `NULL`. Show high-level progress and result
messages. `NULL` (default) inherits `getOption("dw.verbose", TRUE)`;
set `TRUE`/`FALSE` to override for this call. See [dw_verbosity()].}

\item{debug}{Logical or `NULL`. Show internal troubleshooting detail
(resolved paths, dims, branch decisions). `NULL` (default) inherits
`getOption("dw.debug", FALSE)`; implies `verbose`. See [dw_verbosity()].}
}
\value{
A list with `summary` (one-row tibble) and `added`, `removed`,
`changed` data frames.
}
\description{
Three-way comparison of two data frames keyed on `by`, returning the
rows added on the current side, removed on the reference side, and
value-changed rows on the intersection. Numeric value columns can use a
tolerance-based equality; string columns normalise to trimmed lowercase
empty-equivalents (`""`, `"NA"`, `"N/A"`, `"NULL"`, `"."`).
}
\seealso{
[dw_use()] (used to materialise the inputs when paths are
supplied); [dw_merge()] for a Stata-style join with cardinality
assertion.

Other io: 
\code{\link{dw_default_unicef_allowlist}()},
\code{\link{dw_is_canonical}()},
\code{\link{dw_isid}()},
\code{\link{dw_merge}()},
\code{\link{dw_resolve_path}()},
\code{\link{dw_root}()},
\code{\link{dw_save}()},
\code{\link{dw_stage}()},
\code{\link{dw_toolkit_version}()},
\code{\link{dw_use}()},
\code{\link{dw_verbosity}()},
\code{\link{dw_verify_z}()}
}
\concept{io}
