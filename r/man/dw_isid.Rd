% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dw_io.R
\name{dw_isid}
\alias{dw_isid}
\title{Stata-style uniqueness check on a key tuple}
\usage{
dw_isid(df, keys, where = "<unknown>")
}
\arguments{
\item{df}{Data frame to check.}

\item{keys}{Character vector of column names that should uniquely
identify rows.}

\item{where}{Character. Context label included in the error message
(typically the resolved output path).}
}
\value{
Invisibly, `TRUE` when the check passes. Stops with a sample
of duplicates when it fails.
}
\description{
Raises an informative error if `df` has duplicate rows on the supplied
key columns. Inspired by Stata's `isid` (and World Bank's `edukit_save`).
Used by [dw_save()] when an `isid =` argument is supplied.
}
\examples{
\dontrun{
library(dplyr)
df <- data.frame(REF_AREA = c("AGO", "BFA"), value = c(1, 2))
dw_isid(df, keys = "REF_AREA")
}

}
\seealso{
[dw_save()] (auto-invokes `dw_isid` when an `isid =` argument
is passed).

Other io: 
\code{\link{dw_compare}()},
\code{\link{dw_default_unicef_allowlist}()},
\code{\link{dw_is_canonical}()},
\code{\link{dw_merge}()},
\code{\link{dw_resolve_path}()},
\code{\link{dw_root}()},
\code{\link{dw_save}()},
\code{\link{dw_stage}()},
\code{\link{dw_toolkit_version}()},
\code{\link{dw_use}()},
\code{\link{dw_verify_z}()}
}
\concept{io}
