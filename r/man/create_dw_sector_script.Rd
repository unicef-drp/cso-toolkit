% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_sector_script.R
\name{create_dw_sector_script}
\alias{create_dw_sector_script}
\title{Generate a prefilled sector-run script using DW-Production conventions}
\usage{
create_dw_sector_script(
  sector_name,
  sector_code,
  project_root = ".",
  overwrite = FALSE
)
}
\arguments{
\item{sector_name}{Character. Full name of the sector (e.g., "Nutrition",
"WASH").}

\item{sector_code}{Character. Short abbreviation used in the folder
structure and filenames (e.g., "nt", "ws").}

\item{project_root}{Character. Project root the relative `base_dir` is
resolved against. Defaults to `"."` (assumes you are at the
DW-Production repo root when calling).}

\item{overwrite}{Logical. If `FALSE` (default), stops when the target file
already exists. Set `TRUE` to overwrite.}
}
\value{
Invisibly, the path of the file written.
}
\description{
Thin convenience wrapper around [`create_sector_script()`] that fills in
the DW-Production layout: scripts land at
`01_dw_prep/012_codes/<sector_code>/00_run_<sector_code>.R`, input under
`01_dw_prep/011_input/<sector_code>`, output under
`01_dw_prep/013_output/<sector_code>`. Suitable for use inside the
DW-Production repo root.
}
\examples{
\dontrun{
# From the DW-Production repo root, scaffold the WASH sector:
create_dw_sector_script("WASH", "ws")
}
}
