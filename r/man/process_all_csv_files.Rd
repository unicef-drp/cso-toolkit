% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_markdown_report.R
\name{process_all_csv_files}
\alias{process_all_csv_files}
\alias{dw_process_all_csv_files}
\title{Generate descriptive-statistics Markdown reports for every CSV in a folder}
\usage{
process_all_csv_files(
  folder_path,
  country_column,
  year_column,
  indicator_column,
  value_column,
  output_path = NULL,
  verbose = NULL,
  debug = NULL
)

dw_process_all_csv_files(
  folder_path,
  country_column,
  year_column,
  indicator_column,
  value_column,
  output_path = NULL,
  verbose = NULL,
  debug = NULL
)
}
\arguments{
\item{folder_path}{Character. Directory containing input CSVs (not
recursed).}

\item{country_column, year_column, indicator_column, value_column}{Column names — see [generate_markdown_report()].}

\item{output_path}{Character. Output directory for `.md` files. Default
`NULL` writes into the current working directory.}

\item{verbose}{Logical or `NULL`. Show high-level progress and result
messages. `NULL` (default) inherits `getOption("dw.verbose", TRUE)`;
set `TRUE`/`FALSE` to override for this call. See [dw_verbosity()].}

\item{debug}{Logical or `NULL`. Show internal troubleshooting detail
(resolved paths, dims, branch decisions). `NULL` (default) inherits
`getOption("dw.debug", FALSE)`; implies `verbose`. See [dw_verbosity()].}
}
\value{
Invisibly, `NULL`.
}
\description{
Lists every `.csv` file in `folder_path` and calls
[generate_markdown_report()] on each. A thin convenience wrapper.
}
\examples{
\dontrun{
process_all_csv_files(
  folder_path     = "data/raw/",
  country_column   = "countrycode",
  year_column      = "year",
  indicator_column = "indicator",
  value_column     = "value",
  output_path      = "reports/"
)
}
}
\seealso{
[generate_markdown_report()] (single-file engine).

Other reporting: 
\code{\link{generate_markdown_report}()}
}
\concept{reporting}
