% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_data_v2.R
\name{aggregate_data_v2}
\alias{aggregate_data_v2}
\title{Aggregate Data v2.0 - Enhanced for Cross-Sector Use}
\usage{
aggregate_data_v2(
  data,
  value,
  weight,
  by,
  country_id = "REF_AREA",
  global = TRUE,
  method = c("weighted_mean", "mean", "sum", "proportion"),
  coverage_threshold = NULL,
  pop.coverage = TRUE,
  country.coverage = TRUE,
  total.population = FALSE,
  number.affected = FALSE,
  global_label = "WORLD",
  validate = TRUE
)
}
\arguments{
\item{data}{A data frame containing the data to be aggregated.}

\item{value}{Character. Column name containing values to aggregate.}

\item{weight}{Character. Column name containing population weights.}

\item{by}{Character vector. Column names to group by for aggregation.}

\item{country_id}{Character. Column name identifying countries (for country coverage). Default "REF_AREA".}

\item{global}{Logical. Include global aggregation? Default TRUE.}

\item{method}{Character. Aggregation method: "weighted_mean", "mean", "sum", "proportion". Default "weighted_mean".}

\item{coverage_threshold}{Numeric. Minimum population coverage (0-1) to report aggregate. Default NULL (no threshold).}

\item{pop.coverage}{Logical. Return population coverage? Default TRUE.}

\item{country.coverage}{Logical. Return country count? Default TRUE.}

\item{total.population}{Logical. Return total population in group? Default FALSE.}

\item{number.affected}{Logical. Return number affected (for proportion method)? Default FALSE.}

\item{global_label}{Character. Label for global aggregate row. Default "WORLD".}

\item{validate}{Logical. Perform input validation? Default TRUE.}
}
\value{
A data frame with aggregated data including requested metadata columns.
}
\description{
This function aggregates data by specified grouping variables with support for
multiple aggregation methods, coverage thresholds, and automatic metadata generation.
}
\seealso{
[aggregate_data()] for the v1 signature kept for back-compat;
  [generate_agg_footnote()] for the standard footnote string;
  [apply_time_window()] to filter to the latest observation in a window.

Other aggregate: 
\code{\link{aggregate_data}()},
\code{\link{apply_time_window}()},
\code{\link{generate_agg_footnote}()}
}
\concept{aggregate}
