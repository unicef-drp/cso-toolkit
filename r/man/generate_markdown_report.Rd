% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_markdown_report.R
\name{generate_markdown_report}
\alias{generate_markdown_report}
\title{Generate a descriptive-statistics Markdown report from a single CSV file}
\usage{
generate_markdown_report(
  csv_file_path,
  country_column,
  year_column,
  indicator_column,
  value_column,
  output_path = NULL
)
}
\arguments{
\item{csv_file_path}{Character. Path to the input CSV.}

\item{country_column}{Character. Column name holding country identifiers.}

\item{year_column}{Character. Column name holding year values.}

\item{indicator_column}{Character. Column name holding indicator codes.}

\item{value_column}{Character. Column name holding the numeric value to
summarise.}

\item{output_path}{Character. Directory to write `<basename>.md` into.
Default `NULL` writes the file in the current working directory.}
}
\value{
Invisibly, `NULL`. Side effect: writes a `.md` file.
}
\description{
Reads the CSV at `csv_file_path` and writes a Markdown report that
includes:
\itemize{
  \item A general preamble (filename, timestamp, user, number of
        observations, number of unique countries / years / indicators,
        number of variables).
  \item A variable-details table (type, unique cases, mean / SD / min /
        max for numerics).
  \item Optional summary tables by country, year, and indicator (only
        when those columns are present in the input).
}
}
\details{
Columns that are not present are skipped silently — the preamble still
renders.
}
\examples{
\dontrun{
generate_markdown_report(
  "input.csv",
  country_column   = "countrycode",
  year_column      = "year",
  indicator_column = "indicator",
  value_column     = "value",
  output_path      = "reports/"
)
}
}
\seealso{
[process_all_csv_files()] to loop over a folder of CSVs.

Other reporting: 
\code{\link{process_all_csv_files}()}
}
\concept{reporting}
