% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dw_io.R
\name{dw_verify_z}
\alias{dw_verify_z}
\title{Verify a canonical Teams file matches its Z: drive mirror}
\usage{
dw_verify_z(path, compare = c("size", "sha256"))
}
\arguments{
\item{path}{Character. Path to a file under `teamsFolderCanonical`.}

\item{compare}{Character. `"size"` (default, fast) or `"sha256"` (deep,
requires the `digest` package).}
}
\value{
A list with `status` and supporting fields. `status` is one of:
`"no_z_mirror"`, `"z_missing"`, `"match_size"`, `"size_mismatch"`,
`"match_sha256"`, `"sha256_mismatch"`, or `"verify_unavailable"`.
}
\description{
Compares the file at `path` against the corresponding Z: mirror via
either a fast file-size check or a deep sha256 hash. Non-blocking by
design: callers act on the returned `status` rather than seeing an
immediate `stop()`.
}
\seealso{
[dw_save()] (carbon-copies on canonical writes) and [dw_use()]
(runs an automatic size check on canonical reads).

Other io: 
\code{\link{dw_compare}()},
\code{\link{dw_default_unicef_allowlist}()},
\code{\link{dw_is_canonical}()},
\code{\link{dw_isid}()},
\code{\link{dw_merge}()},
\code{\link{dw_resolve_path}()},
\code{\link{dw_save}()},
\code{\link{dw_toolkit_version}()},
\code{\link{dw_use}()}
}
\concept{io}
