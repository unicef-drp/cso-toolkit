% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile_helpers.R
\name{create_profile}
\alias{create_profile}
\alias{dw_create_profile}
\title{Generate a project profile script for a CSO data-warehouse pipeline}
\usage{
create_profile(
  repo_name,
  project_title = repo_name,
  output_path = ".",
  include_dw_mode = TRUE,
  include_z_drive_check = FALSE,
  author = Sys.getenv("USERNAME", unset = Sys.info()[["user"]]),
  overwrite = FALSE
)

dw_create_profile(
  repo_name,
  project_title = repo_name,
  output_path = ".",
  include_dw_mode = TRUE,
  include_z_drive_check = FALSE,
  author = Sys.getenv("USERNAME", unset = Sys.info()[["user"]]),
  overwrite = FALSE
)
}
\arguments{
\item{repo_name}{Character. Repository folder name, used in the sentinel
object name (`profile_<repo_name>` with `-` and `.` converted to `_`)
and the generated filename (`profile_<repo_name>.R`).}

\item{project_title}{Character. Human-readable project title for the
header block (e.g., "UNICEF DW Production"). Defaults to `repo_name`.}

\item{output_path}{Character. Directory the file is written to. Defaults
to `"."`.}

\item{include_dw_mode}{Logical. Include the producer / reviewer mode
block that reads `dw_mode` from `user_config.yml` and hard-fails when
missing? Default `TRUE`.}

\item{include_z_drive_check}{Logical. Include the Z: drive availability
advisory block? Default `FALSE` (DW-Production-specific).}

\item{author}{Character. Author name for the header. Defaults to the
system user.}

\item{overwrite}{Logical. If `FALSE` (default), stops when the target
file already exists.}
}
\value{
Invisibly, the absolute path of the file written.
}
\description{
Writes a `profile_<repo_name>.R` template to disk. The template wires up
the standard CSO building blocks: user identification (USERNAME / USER
for Windows / Mac), reproducibility seed, optional Z: drive integrity
check, YAML user config load, optional producer / reviewer dw_mode
resolution, a placeholder packages block, and the profile sentinel object
downstream sector scripts assert against.
}
\examples{
\dontrun{
create_profile("My-Sector-Project", project_title = "My Sector Project")
create_profile("DW-Production",
               project_title = "UNICEF DW Production",
               include_z_drive_check = TRUE,
               overwrite = TRUE)
}
}
\seealso{
[review_profile()] to audit the generated profile against the
  toolkit contract; [create_sector_script()] for sector-level
  scaffolding that depends on the profile sentinel.

Other scaffolding: 
\code{\link{create_dw_sector_script}()},
\code{\link{create_sector_script}()},
\code{\link{review_profile}()}
}
\concept{scaffolding}
