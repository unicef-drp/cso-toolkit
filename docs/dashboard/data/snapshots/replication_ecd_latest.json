{
  "_schema_version": "1.0.0",
  "sector": "ecd",
  "last_run_at": "2026-05-29T00:00:00Z",
  "last_run_by": "reviewer",
  "status": "BLOCKED",
  "halt_phase": "env-setup",
  "halt_reason": "An ECD prep script hardcodes input/output directories to a producer-side personal path, so none of them resolve on the reviewer's machine. Additionally, a sector script depends on library(xlsx), which requires Java (unavailable on the reviewer machine), same as wt.",
  "fixes_attempted": [
    "Identified the hardcoded producer-side paths in a sector script (input/output/working/region-group directories)",
    "Searched a shared Teams folder for the source workbook referenced by the indicator scripts — the workbook does not exist in shared storage (only unrelated CSV variants are present)",
    "Searched for ecd-specific raw inputs — none located at the path the script expects"
  ],
  "blockers_for_dbm": "Four DBM-actionable items:\n  1. CRITICAL: Replace the hardcoded producer-side paths in a sector script with profile-driven globals (dwRawData, dwCodes, dwWrkData, dwMetaData). The current code is portable only on one producer's machine.\n  2. Deposit the producer-side source workbook (referenced by all six ECD indicator scripts) to a shared canonical location so reviewers can read it. The workbook is currently only on the producer side.\n  3. Java install OR xlsx -> openxlsx port (same as wt).\n  4. The script bypasses cso-toolkit entirely (no dw_use, dw_save). For v0.4.x compliance the sector lead must adopt dw_io contract."
}
