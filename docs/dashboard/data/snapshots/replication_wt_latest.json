{
  "_schema_version": "1.0.0",
  "sector": "wt",
  "last_run_at": "2026-05-29T00:00:00Z",
  "last_run_by": "reviewer",
  "status": "BLOCKED",
  "halt_phase": "package-load + env-setup",
  "halt_reason_1": "library(xlsx) requires Java runtime; Java not available in the reviewer environment",
  "halt_reason_2": "After patching to library(openxlsx), the script's column selection (Region.Name, M49.Code.region., etc.) does not match the actual Teams xlsx structure (M49, Name, Ref_Area_Type, RC_Region, RC_UNSDCode, RC_RegionName, RC_Name across 7 cols x 1331 rows). xlsx::read.xlsx2 and openxlsx::read.xlsx have different default column-name handling AND the producer's working version of the xlsx may have a different schema",
  "halt_reason_3": "Even with package + column-name issues fixed, the regional_classification.R script calls assert_required_files() listing population.rds at <teamsWrkData>/CPD/wt/ as required — that producer-side cached output does not exist on Teams. Producer must run a prior cme/cpd cache step first.",
  "fixes_attempted": [
    "Installed xlsx + summarytools + xlsxjars + matrixStats + pander + rapportools (xlsx still fails on Java load)",
    "Patched library(xlsx) -> library(openxlsx) in wt_dw.R + population.R",
    "Wrote read.xlsx2 -> openxlsx::read.xlsx shim in driver",
    "Removed rm(list=ls(all=TRUE)) in wt_dw.R that wipes driver-set globals",
    "Set required_vars globals (teamsRawData, teamsWrkData, dwCodes, githubFolder) explicitly"
  ],
  "blockers_for_dbm": "Three DBM-actionable items:\n  1. Java dependency — sector lead must port wt_dw.R + sub-scripts from xlsx to openxlsx (recommended path; openxlsx is pure-R, no Java dependency) so the pipeline runs in environments without a Java runtime.\n  2. xlsx file schema mismatch — sector code expects columns Region.Name, M49.Code.region., X2025.Submission..if.data.submission.is.required. but the canonical Teams xlsx '9. CompositionOfRegions_RCs_20241219.xlsx' has columns M49, Name, Ref_Area_Type, RC_Region, RC_UNSDCode, RC_RegionName, RC_Name. Either the sector code is reading the wrong sheet/file, OR the producer's working xlsx differs from the Teams-deposited copy. Sector lead must reconcile.\n  3. Upstream cache dependency — wt regional_classification.R requires <teamsWrkData>/CPD/wt/population.rds, a producer-side cached artifact not present on Teams. Producer must commit this artifact OR document how the reviewer regenerates it before wt can run."
}
