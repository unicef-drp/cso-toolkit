id: ed-wb-api-allowlist
title: "ED: World Bank API access — allowlist or cache"
sector: ed
severity: HIGH
status: TODO
owner: "UNICEF IT (via DBM ticket)"
created_at: "2026-05-30"
due_at: null
blocker_for_dbm: "The ED pipeline halts mid-run because upstream World Bank API requests are throttled on the reviewer network."
description: "ED steps for Learning Poverty / FLS require the World Bank API, which is throttled on the reviewer network. Options: (a) allowlist the API; (b) cache the data once off-network and commit a frozen snapshot; (c) run those steps off the corporate network. Detail in the private DW-Production tracker."
references:
  - "replication_ed_latest.json: halt_reason"
